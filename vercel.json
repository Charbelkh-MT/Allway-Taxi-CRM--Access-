{
  "buildCommand": "cd allway-crm && npm install && npm run build",
  "outputDirectory": "allway-crm/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "crons": [
    {
      "path": "/api/send-daily-report",
      "schedule": "0 18 * * *"
    }
  ]
}
